namespace WeatherApp.Tests;

/// Proves the scaffolded substrate runs: the solution builds and the xUnit
/// runner discovers and executes a test. Later stories replace this with real
/// behavioural tests against WeatherApp.Core.
public class SmokeTest
{
    [Fact]
    public void Solution_builds_and_tests_run() => Assert.True(true);
}
