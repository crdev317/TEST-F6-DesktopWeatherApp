# Artefact Consistency Check — 2026-06-30 (scoped to Feature 1 — #95088)

> Gauntlet-scoped run. Not a project-wide sweep — limited to Feature 1's Spec and Plan
> plus the upstream artefacts they reference (business-domain-context.md, Technical-Context.MD,
> PRD.md, Roadmap.md F1 entry, ADR-0001).

**Artefacts read:**
- `business-domain-context.md` (7 terms: Condition, Current Conditions, Forecast, Geocoder, Location, Location Search, Weather Provider)
- `Technical-Context.MD` (1 Overriding Principle — #1 No secrets in source)
- `PRD.md` (22 user stories)
- `Roadmap.md` (6 Features)
- `docs/superpowers/specs/2026-06-29-feature1-current-weather-design.md` (F1 spec)
- `docs/superpowers/plans/2026-06-29-feature1-current-weather.md` (F1 plan)
- `docs/adr/0001-persist-location-only-never-cache-weather.md` (ADR-0001)

**Summary:** 0 contradictions, 0 gating vocabulary drifts (2 non-gating naming notes), 0 orphans, 0 coverage gaps. 1 non-gating disclosed-deferral note.

---

## Contradictions

No contradictions found.

- **ADR-0001 (never cache weather) vs F1 Spec/Plan.** `WeatherViewModel.Load` always fetches fresh;
  no Location Store / persistence / cache in F1. Honoured.
- **ADR-0001 keep-last-good + Updated-at (Consequences) vs F1.** F1 defers refresh / Updated-at /
  keep-last-good to F4 (Spec "Deliberately absent"; Roadmap F1 out-of-scope). F1 never reaches that
  behaviour. No conflict.
- **Glossary vs Spec/Plan.** Condition, Current Conditions, Geocoder, Weather Provider, Location,
  Location Search used as defined.
- **PRD vs Spec/Plan.** Metric units, explicit candidate selection, single-result-confirmed, inline
  errors, empty state — all consistent.

## Vocabulary drift

No gating drift. Two non-gating naming notes:

### V1 (non-gating): "Weather Provider" (service) vs "Weather Provider client" (module)
Glossary defines **Weather Provider** as the external service; PRD/Spec/Plan use **Weather Provider
client** for the wrapping module. Consistent across PRD→Spec→Plan. No action required.

### V2 (non-gating): "debounce clock" vs "debounce scheduler"
Spec says "injected debounce clock"; Plan implements `IDebounceScheduler`. Same concept, two labels.
Already swept as a cosmetic naming observation in the Spec sign-off history.

## Orphaned references

No orphaned references found. Every Context-reference path in the F1 Spec and Plan resolves on disk;
Spec Seam 3 (d) "(Plan Task 5)" resolves to the `Formats_coordinates_invariantly...` test; all
forward Features the Spec defers to (F2/F3/F4/F5/F6) exist in the Roadmap; Roadmap F1 → ADO #95088
matches the feature under review.

## Coverage gaps

No coverage gaps found (within F1 scope). F1 has a Spec and Plan; all three Spec seams are covered by
Plan tasks (Seam 1→Task 4, Seams 2 & 3→Task 5); F1-scoped PRD requirements map to Spec behaviour and
Plan tasks; later-Feature PRD reqs are correctly out of F1; Overriding Principle #1 (no secrets) is
enforced by the gitleaks `ci` gate and confirmed by the Plan self-review.

## Non-gating note (disclosed deferral)

### N1: Per-call Open-Meteo request logging deferred for F1
`Technical-Context.MD` §Instrumentation asks that every outbound Open-Meteo call be logged with its
outcome — a line that doc marks as an "_Inferred default_", not an Overriding Principle. The F1 Spec
explicitly defers per-call logging for the tracer bullet (clients take no `ILogger`), keeping the
generic-host logging substrate wired so the obligation can be picked up cheaply later. Disclosed
deferral against a non-prescriptive line — non-gating observation, not a contradiction.
